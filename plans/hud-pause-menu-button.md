# HUD Pause Menu Button Plan

## Goal

Add a HUD pause/settings icon button below the overlay controls that opens the existing pause menu.

## Phases

1. Add the gear texture as an external resource in the current game scene.
2. Add a new HUD section below overlay buttons with an icon-only button.
3. Connect the button to the existing pause menu.
4. Validate the current game scene loads.
