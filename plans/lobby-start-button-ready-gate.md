# Lobby Start Button Ready Gate Plan

## Goal

Disable the lobby Start button until the server-confirmed lobby state meets the same ready-player condition enforced by the game server.

## Phases

1. Confirm the server-side start condition.
2. Add a matching client-side helper in the lobby UI.
3. Update the Start button state during lobby refresh.
4. Run targeted validation.
