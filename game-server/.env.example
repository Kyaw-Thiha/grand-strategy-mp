NODE_ENV=production
PORT=2567

# Must match api-server JWT_SECRET exactly
JWT_SECRET=

# Guards /internal/* routes — must match api-server INTERNAL_SECRET
# openssl rand -base64 32
INTERNAL_SECRET=

# Base URL of the Hono api-server (Railway service URL)
HONO_URL=

